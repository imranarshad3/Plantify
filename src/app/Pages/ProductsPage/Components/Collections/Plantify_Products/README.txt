Plantify Products

Copy the `products` folder into your project's `public` folder and copy `data.js` into `src`.

Recommended structure:
public/products/*.png
src/data.js

The image paths in data.js are already configured as /products/filename.png.
